/**
 * Represents a rotation with x and y coordinates.
 *
 * @typedef {Object} Rotation
 * @property {number} x - The x-coordinate of the rotation.
 * @property {number} y - The y-coordinate of the rotation.
 */
export type Rotation = {
    x: number;
    y: number;
};
